import React from 'react';
import './hero.css';

const HeroSection = () => {
  return (
    <div className="hero-section">
      <h1>Front-end developer test project</h1>
      <p>My goal was to make a page that looks exactly like this one, 
        and has the ability to create H1 text simply by typing / then 1, then typing text and hitting enter.</p>
    </div>
  );
};

export default HeroSection;
